spring:
  data:
    mongodb:
      database: testdb
  shell:
    interactive:
      enabled: false
    script:
      enabled: false

logging:
  level:
    org.mongodb.driver.cluster: error

mongock:
  enabled: false
  migration-scan-package:
    - ru.otus.hw.migrations