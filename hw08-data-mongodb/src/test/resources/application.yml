spring:
  data:
    mongodb:
      database: testdb
  shell:
    interactive:
      enabled: false
    script:
      enabled: false

logging:
  level:
    org.mongodb.driver.cluster: error

mongock:
  enabled: true
  migration-scan-package:
    - ru.otus.hw.migrations