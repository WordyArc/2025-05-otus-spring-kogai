spring:
  data:
    mongodb:
      database: testdb
  shell:
    interactive:
      enabled: false
    script:
      enabled: false

de.flapdoodle.mongodb.embedded.version: 7.0.4
mongock:
  migration-scan-package:
    - ru.otus.hw.migrations