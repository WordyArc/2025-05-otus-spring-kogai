<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">
    <springProperty scope="context" name="LOG_DIR"  source="app.logging.dir"  defaultValue="/app/logs"/>
    <springProperty scope="context" name="LOG_FILE" source="app.logging.file" defaultValue="application.log"/>

    <property name="FULL_LOG_PATH" value="${LOG_DIR}/${LOG_FILE}"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{ISO8601} %highlight([%level]) %yellow([%t]) [%cyan(%C::%M:%L)] | %m %n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${FULL_LOG_PATH}</file>
        <append>true</append>
        <encoder>
            <pattern>%d{ISO8601} [%level] [%t] [%C{1}.%M:%L] | %m%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/application.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>